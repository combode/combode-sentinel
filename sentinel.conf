# specify path to combode.conf or leave blank
# default is the same as CombodeCore
combode_conf=/root/.combodecore/combode.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
#network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
